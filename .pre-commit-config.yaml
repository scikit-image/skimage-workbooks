# See https://pre-commit.com; enable with `pre-commit install`.
repos:
  - repo: local
    hooks:
      - id: environment-yml
        name: regenerate environment.yml from build_requirements.txt
        entry: make -B environment.yml
        language: system
        pass_filenames: false
        files: ^(build_requirements\.txt|environment\.yml)$
